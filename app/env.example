# Backend settings
DEVICE=cuda:0
RETENTION_ROOT=/srv/faceverify/data
# If serving UI via FastAPI, set STATIC_DIR to your built frontend dist
# STATIC_DIR=/Users/ravee/Documents/GitHub/FaceVerify/frontend/dist
MAX_STREAMS=5

# Embedder via state_dict (preferred in your case)
EMBEDDER_STATE_DICT=/Users/ravee/Documents/GitHub/FaceVerify/models/face/model.pt
EMBEDDER_ARCH=r100

# Or, alternatively, a TorchScript model
# EMBEDDER_TORCHSCRIPT=/absolute/path/to/your_embedder.ts_or_pt
